#pragma once
template <class K, class V>
/*
class Map // like a hash_map
{
	// map = multiple entries(key) / key = multiple values
	constexpr int maximum = 300;
	//key* first_entry;

public:
	Map();
	void Set();
	bool Get(const K& key, V& value); //copy the value associated to parameter key into the parameter value and returns true.
	bool Includes(const Map<K, V>& map);

	V operator [] (K key);

	entry* begin()const;
	entry* end()const;

	int  Count()const;
	void Clear(); // delete everything
	bool Delete(const& K key); //just one entry
	~Map();
};
*/